import React from 'react'
import { DefaultTemplate } from 'components/templates'
import { Tasks } from 'modules'

const Home = () => (
  <DefaultTemplate>
    <Tasks />
  </DefaultTemplate>
)

export default Home
